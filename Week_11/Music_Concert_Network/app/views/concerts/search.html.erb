<h1>Search Results</h1>
<% @concert_filter.each do | concert | %>
	<ul>
		<li>
			<%= link_to(concert.artist, concert_path(concert.id)) %>
		</li>
	</ul>
	<% end %>

<%= link_to("View All Concerts", concerts_path) %>